//Paragraph to be rendered from mysql db, It should only get body content and concatenate to 128 characters. 
 
 <p>  
 <?php echo substr($row_blog['body'], 0, 128) . '..';?> // PHP line to render text from db.                         
 </p>
                  
